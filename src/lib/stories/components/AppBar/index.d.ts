export interface AppBarProps {
    items: any[];
    selectedIndex?: number;
}
export declare const AppBar: ({ items, selectedIndex, ...props }: AppBarProps) => import("@emotion/react/jsx-runtime").JSX.Element;
