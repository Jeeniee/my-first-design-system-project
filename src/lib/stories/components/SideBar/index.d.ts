export interface SideBarProps {
    items: any[];
    selectedIndex?: number;
}
export declare const SideBar: ({ items, selectedIndex, ...props }: SideBarProps) => import("@emotion/react/jsx-runtime").JSX.Element;
