import { css } from "@emotion/react";
import { foundation } from "lib";

const ContainerStyle = css`
  max-width: 100%;
  margin: 0 auto;
  padding: 0 ${foundation.theme.spacing * 5}px;
`;

export { ContainerStyle };
